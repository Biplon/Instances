package instance.java.Effect;

import org.bukkit.Location;

public class EffectSchematicRemove extends EffectLocation
{
    public EffectSchematicRemove(int id, boolean hasPos, boolean allPlayer, double timer)
    {
        super(id, hasPos, allPlayer, timer);
    }

    @Override
    public void startEffect(Location pos)
    {

    }
}
